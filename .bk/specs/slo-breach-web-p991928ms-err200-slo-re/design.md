# Design: SLO breach: web (P99=1928ms, err=2.00%)

## SLO report: `web`

- Samples: 10 lat

## Architecture

### Components
- **SLOBreachReport** (component): Component to display SLO breach details and metrics
- **MetricsDisplay** (component): Reusable component to show metrics with thresholds and breaches
- **RecommendationsList** (component): Component to display SLO breach recommendations
- **CloudFrontService** (service): Service to manage CloudFront edge caching configuration
- **RedisCache** (service): Service to handle Redis caching operations
- **CircuitBreaker** (service): Service implementing circuit breaker pattern for downstream calls


### API Contracts
- `GET /api/slo/breaches/{service}` — Get SLO breach details for a service


### Data Flow
1. Client sends request to API gateway
2. API routes to appropriate service
3. Service processes business logic
4. Data persisted to database
5. Response returned to client

## Technology Stack
- Frontend: Next.js 14+ with TypeScript, React, Tailwind CSS
- Backend: ASP.NET Core 8+ with C#, Entity Framework Core
- Database: PostgreSQL
- Infrastructure: AWS EKS, Terraform

## Security Considerations
- Input validation on all endpoints
- Authentication via JWT tokens
- Rate limiting on public APIs
- Encrypted data at rest and in transit

---
*Generated by DevOps Agentic Teammates — Plan & Collaborate Agent*
